---
const { step, title, description, timeline, isUnfold } = Astro.props;
---

<div
  class="w-[300px] min-h-[440px] bg-black/50 p-4 flex flex-col items-center gap-10"
>
  <div
    class="w-6 h-6 flex items-center justify-center text-greencell border-2 border-greencell rounded-full"
  >
    {step}
  </div>
  <div class="flex flex-col flex-1 items-center justify-between">
    <h3 class="text-xl font-bold uppercase text-center">{title}:</h3>
    <p class="text-center">{description}</p>
    <p>Timeline: <span class="uppercase text-greencell">{timeline}</span></p>
  </div>
</div>
